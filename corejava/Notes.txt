i) -> one error that i encountered is NoSuchError which happened due to me using Class Mobile and Class OnePlus in two different java files and the methods I defined was different in those classes.
   -> So i changed the class name in another file so that all class names are different.